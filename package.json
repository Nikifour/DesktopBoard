{
  "name": "desktop-board",
  "version": "0.2.2",
  "description": "A Windows desktop board for notes, schedule blocks, and quick personal widgets.",
  "author": "Desktop Board",
  "main": "main.js",
  "private": true,
  "scripts": {
    "dev": "electron .",
    "preview": "node scripts/serve.js",
    "check": "node --check main.js && node --check preload.js && node --check src/renderer.js && node --check scripts/serve.js && node --check scripts/after-pack.js",
    "dist": "electron-builder",
    "dist:win": "set ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/&& set CSC_IDENTITY_AUTO_DISCOVERY=false&& electron-builder --win",
    "dist:win:publish": "set ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/&& set CSC_IDENTITY_AUTO_DISCOVERY=false&& electron-builder --win --publish always"
  },
  "build": {
    "appId": "com.desktopboard.app",
    "productName": "Desktop Board",
    "electronVersion": "41.2.1",
    "electronDist": "node_modules/electron/dist",
    "electronUpdaterCompatibility": ">=2.16",
    "afterPack": "scripts/after-pack.js",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "src/native",
        "to": "native",
        "filter": [
          "**/*"
        ]
      }
    ],
    "fileAssociations": [
      {
        "ext": "desktopboard",
        "name": "Desktop Board Archive",
        "description": "Desktop Board board archive",
        "icon": "icon.ico",
        "role": "Editor"
      }
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "Nikifour",
        "repo": "DesktopBoard",
        "releaseType": "release"
      }
    ],
    "win": {
      "icon": "icon.ico",
      "signAndEditExecutable": false,
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "installerIcon": "icon.ico",
      "uninstallerIcon": "icon.ico",
      "installerHeaderIcon": "icon.ico",
      "artifactName": "Desktop-Board-Setup-${version}.${ext}",
      "shortcutName": "Desktop Board"
    },
    "portable": {
      "artifactName": "Desktop-Board-${version}.${ext}"
    }
  },
  "devDependencies": {
    "electron": "41.2.1",
    "electron-builder": "^26.8.1",
    "rcedit": "^5.0.2"
  },
  "dependencies": {
    "electron-updater": "6.8.3",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "xlsx": "^0.18.5"
  }
}
